# LEO-Q: 8 Quantum-Test Datasets (CSV, min 50 records each)
Generated: 2026-01-28T02:12:29.276478Z

All 8 datasets in this folder have >=50 rows as requested.

## Files
1. dataset1_var_returns.csv
   - Correlated returns (scenarios x assets) + portfolio_loss.

2. dataset2_heavytail_returns.csv
   - Heavy-tail returns with regime labels + jump_flag + portfolio_loss.

3. dataset3_loss_surface.csv
   - Loss surface samples over (s, v) with randomized r, t and portfolio_loss.

4. dataset4_qaoa_instances.csv
   - Flattened QAOA instances: one row per asset with instance-level constraints.

5. dataset5_regime_switch.csv
   - Daily regime schedule with avg_corr and volatility multiplier.

6. dataset6_qkd_sessions.csv
   - QKD session overhead synthetic logs.

7. dataset7_routing_e2e.csv
   - End-to-end compute+route+security overhead per pair.

8. dataset8_noise_profiles.csv
   - Quantum noise profiles for sensitivity tests.
