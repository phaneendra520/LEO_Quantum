See `src/LEOQ.Core/Experiments/` for implementation.
See `docs/experiment_notes.md` for full technical notes.
See `results/` for pre-computed CSV outputs.

Run:  dotnet run --project src/LEOQ.Cli -- qaoa --layers 2 --shots 4096 --seed 42
